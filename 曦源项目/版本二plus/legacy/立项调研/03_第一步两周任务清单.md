---
create time: 2026-05-27T15:30:00
tags:
  - 曦源项目
  - 版本二plus
  - 任务清单
  - W1
  - W2
  - 立项后启动
status: 工作版v1
audience: 项目组（A 工程同学 + B 评测同学）/ 项目执行 day-by-day 参考
---

# 03 · 第一步两周任务清单（W1-W2）

> **目标**：在 2026-05-末 / 06-初的 2 周内完成 P0 阶段，使 v2plus 进入 P1 复现阶段。**到 N0 末（W2 D14）必须达成**：3 个核心仓库装通（OpenVLA-OFT + FastVGGT + LeRobot 真机）+ FastVGGT 在 LIBERO 上 sanity check 通过 + DINOv2 attention 提取跑通 + 真机平台 1 demo 录制成功。
>
> **v2plus 纯净路径**：v2plus 仅依赖 v2 内工具 + FastVGGT。**不引入 SAM2 / GroundedSAM**（属 v3 工具链）。Focus mask 来源 = 方向词 GT 投影（Source A，v2 内）+ DINOv2 CLS attention（Source C，已在 OpenVLA-OFT vision encoder 中）。

## § 1 · 总览

### 1.1 P0 阶段 14 天任务地图

```
W1 D1   D2   D3   D4   D5   D6   D7
        │    │    │    │    │    │    │
       A 工程主导                  B 评测主导
       OpenVLA-OFT 环境          方向词 GT 标注脚本
       FastVGGT 装通             DINOv2 attention 提取
       FastVGGT sanity            Focus mask Source A 设计

W2 D8   D9   D10  D11  D12  D13  D14
        │    │    │    │    │    │    │
       真机平台测试              Focus mask Source C 实现
       1 demo 录制               Focus mask 双源融合 pipeline
       FSF projector 代码         N0 决策
```

### 1.2 每日任务对应表（高层）

| Day | A 工程主导 | B 评测主导 | 共同任务 |
|---|---|---|---|
| D1 | OpenVLA-OFT clone + env setup | 阅读 SF 论文 + 仓库结构 | 团队启动会 1h |
| D2 | OpenVLA-OFT vision encoder 前向 + Layer 24 hook | 阅读 FocusVLA 论文（VGGT 用法对比） | — |
| D3 | LoRA 微调骨架（5 ep sanity） | 方向词 GT 标注脚本（继承 v2）骨架 | — |
| D4 | FastVGGT clone + env setup | 方向词 GT 在 LIBERO 4 suite × 10 demo 跑通 | — |
| D5 | FastVGGT 在 5 张 LIBERO 图前向 | DINOv2 CLS attention 提取脚本（已在 OpenVLA-OFT 内的 DINOv2） | 周末 review |
| D6 | FastVGGT 输出可视化 + GT depth 对比 | Focus mask Source A pipeline（方向词 GT 投影 2D Gaussian） | — |
| D7 | **VGGT vs FastVGGT 输出对比** | Source A 在 10 张 LIBERO 图上目视验证 | **W1 中期 review** |
| D8 | 真机平台 setup（含相机标定） | Focus mask Source C 在 10 张 LIBERO 图上输出 + 与 Source A 对比 | — |
| D9 | 真机 1 demo 录制（pick-place block） | Focus mask Source A + C 集成调试 | — |
| D10 | 真机数据格式与 LIBERO 数据格式对齐 | Focus mask 双源 F1 融合 pipeline（0.5·A + 0.5·C 三段量化） | — |
| D11 | FSF projector 代码（~30 行） | Focus mask 在 50 帧上初步目视验证 | — |
| D12 | FSF projector 在小 batch 训练 50 step | Focus mask 阈值与 bg_w 调试 | — |
| D13 | VGGT 缓存 pipeline 设计（dry-run 100 frame） | Focus mask 在 100 帧上完整目视验证 + 可视化 PDF | — |
| D14 | **N0 决策会**：判断是否进入 P1 | **N0 决策会** | **W2 末决策** |

## § 2 · W1 详细任务（D1-D7）

### W1 D1（周一）· 启动 + OpenVLA-OFT 环境

#### 1.1 团队启动会（1 小时，共同）

- 全员阅读：[[00_总览与立项决定]] + [[01_主方案_FocusedSpatialForcing]] + [[09_与版本二差异化说明]]
- 同步：用户已确认的 5 个关键决策
- 分工确认：A 工程 + B 评测
- 沟通频率：每日 15 分钟站会（晚上 9 点），每周日 review 会（1 小时）

#### 1.2 A 同学：OpenVLA-OFT 环境

```bash
# 创建 conda env
conda create -n openvla python=3.10 -y
conda activate openvla

# Clone 主仓库
git clone https://github.com/openvla/openvla.git
cd openvla
pip install -e .

# 安装依赖
pip install transformers==4.40.1 peft==0.11.1 accelerate==0.30.1
pip install flash-attn==2.5.5 --no-build-isolation

# Verify
python -c "from transformers import AutoModelForVision2Seq; print('OK')"
```

**产出**：环境装通确认；写一个 `setup_check.py` 跑通 OpenVLA-OFT 7B 在 1 张图上的 forward

#### 1.3 B 同学：阅读 Spatial Forcing 论文 + 仓库

- 论文：arXiv:2510.12276（精读 §3 方法学 + §4 实验）
- 仓库：github.com/OpenHelix-Team/Spatial-Forcing（阅读 README + 主代码文件）
- 输出：1 页 SF 方法学摘要（自己写，不抄）

### W1 D2（周二）· OpenVLA-OFT vision encoder + FocusVLA 论文

#### 2.1 A 同学

- OpenVLA-OFT vision encoder 前向跑通
- 拿到 Layer 24 的 hidden state 形状（应为 (B, 256, 4096)）
- **测试 hook**：在 Llama2-7B Layer 24 加 forward hook 抓 hidden state（关键，FSF 实现依赖此）

#### 2.2 B 同学

- 论文：arXiv:2603.28740 FocusVLA（重点 §3 Method）
- 关注：VGGT 在 FocusVLA 中的精确用法（与 SF 的对比）
- 输出：1 页 FocusVLA 中 VGGT 用法摘要

### W1 D3（周三）· LoRA 骨架 + 方向词 GT 标注脚本

#### 3.1 A 同学

- LoRA 微调骨架（用 PEFT 库）
- target_modules: q_proj, k_proj, v_proj, o_proj
- 在 LIBERO-Spatial 1 demo 上跑 5 step（仅 sanity，不要看 SR）
- **关键产出**：能跑通 forward + backward，不报 OOM

#### 3.2 B 同学

- **方向词 GT 标注脚本（继承 v2）**：实现 `annotate_directions(demo_trajectory)` 函数骨架（详见附录 A）
- 输入：LIBERO hdf5 demo
- 输出：每帧的方向词 ∈ {right, left, up, down, front, back, grasped}
- **不装 SAM2 / GroundedSAM**（v2 纯净路径已在 § 1.1 说明）

### W1 D4（周四）· FastVGGT 装通 + 方向词 GT 验证

#### 4.1 A 同学

- FastVGGT 装通：clone https://github.com/mystorm16/FastVGGT
- 装 VGGT 主仓：clone https://github.com/facebookresearch/vggt
- Verify：在 1 张图上跑通 FastVGGT，记录输出 token shape 与维度（应为 (256, 1024)）

#### 4.2 B 同学

- 方向词 GT 脚本在 LIBERO 4 suite × 10 demo 上跑通
- 目视抽样 20 步检查方向词准确率（目标 > 80%，与 v2 一致）
- 整理失败案例（如物体被遮挡时投影失效）

### W1 D5（周五）· FastVGGT 在 LIBERO 上 sanity + DINOv2 attention 提取

#### 5.1 A 同学：FastVGGT 在 5 张 LIBERO 图上前向

```python
import torch
from fastvggt import FastVGGT

model = FastVGGT.load_pretrained('fastvggt-base')
model.eval().cuda()

# 5 张 LIBERO 渲染图（从 4 suite 各取 1 张 + 真机 1 张 dummy）
images = load_libero_renders(n=5)  # (5, 3, 224, 224)
with torch.no_grad():
    outputs = model(images, return_features_at_layer=23)
    # outputs.depth: (5, 224, 224)
    # outputs.point_map: (5, 224, 224, 3)
    # outputs.aggregator_features[23]: (5, 256, 1024)
```

**关键产出**：
- 5 张图的 depth map 可视化（保存为 png）
- aggregator Layer 23 feature shape 确认
- 与原版 VGGT 输出对比（验证 FastVGGT 4× 加速且无损精度）

#### 5.2 B 同学：DINOv2 CLS attention 提取脚本

- DINOv2 已在 OpenVLA-OFT vision encoder 中（与 SigLIP 并行），直接 hook 拿最后一层 self-attention
- 实现 `focus_mask_source_C(image, dinov2)`: 取 CLS-to-patch attention（256 维），min-max 归一化到 [0, 1]
- 在 5 张 LIBERO 渲染图上前向 + 可视化（heatmap 覆盖原图）
- **关键产出**：256 维 attention 向量 + 5 张图的可视化 PDF；判断 DINOv2 saliency 是否对 LIBERO 物体合理（聚焦目标物体而非背景）

#### 5.3 周末 review（30 分钟）

- 同步 W1 D1-D5 完成情况
- 列出 D6-D7 优先级

### W1 D6（周六）· FastVGGT 深度对比 + Focus mask Source A pipeline

- A 同学：把 FastVGGT 在 5 张图上的 depth map 与 ground truth depth（LIBERO 仿真自带）做对比，计算 depth mIoU（目标 ≥ 0.5）
- B 同学：实现 Focus mask Source A pipeline `focus_mask_source_A(p_eef, p_obj, K, R, t)`（详见 [[01_主方案_FocusedSpatialForcing]] § 3.1）。在 10 张 LIBERO 图上目视验证 2D Gaussian 集中在 eef 与 object 位置

### W1 D7（周日）· VGGT vs FastVGGT 对比 + Source A 验证

- A 同学：跑原版 VGGT 在同 5 张图上，对比 FastVGGT 输出差异（应 < 5%）
- B 同学：Source A 在 10 张 LIBERO 图上的输出可视化 + 与 Source C（DINOv2 attention）目视对比（互补性检查）
- **W1 中期 review**（共同 1 小时）：
  - 是否 3 仓库都装通（OpenVLA-OFT + FastVGGT + LeRobot 真机）？
  - FastVGGT 在 LIBERO 上 sanity 通过（mIoU ≥ 0.5）？
  - DINOv2 attention 提取 + 方向词 GT 投影 都能跑？
  - 有无 blocker？

## § 3 · W2 详细任务（D8-D14）

### W2 D8（周一）· 真机平台 + 方向词 GT

#### 8.1 A 同学：真机平台 setup

- 与实验室确认机械臂型号（推荐 SO-100、Franka、AgileX 等）
- 相机内外参标定（用棋盘格，约 30 分钟）
- 真机控制接口跑通（LeRobot / 自定义 SDK）
- **关键产出**：能用 keyboard 遥操作机械臂

#### 8.2 B 同学：方向词 GT 标注脚本

- 直接复用 v2 的方向词标注脚本（详见附录 A）
- 在 LIBERO 4 suite × 10 demo 上跑通
- **关键产出**：每步 1 个方向词（7-class），方向词正确率 > 80%（目视抽 20 步）

### W2 D9（周二）· 真机 demo 录制 + Source A/C 集成

#### 9.1 A 同学：1 个真机 demo 录制

- 任务：pick-place block to bowl
- 录制：人工遥操作 3 次成功 + 1 次失败，记录 obs + action 序列
- 数据格式：与 LIBERO hdf5 对齐（obs/action/eef_pos/object_pos 等）

#### 9.2 B 同学：Focus mask Source A + C 集成调试

- 把 W1 完成的 Source A（方向词 GT 投影）与 Source C（DINOv2 attention）合到一个 pipeline
- 输出 14×14 patch grid（与 OpenVLA-OFT vision patch 对齐）
- **关键产出**：能从单张 RGB + (eef pose, object pose, K, R, t) 同时拿到 m_A 与 m_C

### W2 D10（周三）· 数据对齐 + 双源融合 F1 实现

#### 10.1 A 同学：真机数据 vs LIBERO 数据格式对齐

- 用同一 dataset class 加载 LIBERO 与真机数据
- 保证 trainer 不区分两者

#### 10.2 B 同学：双源融合 F1 pipeline

- 实现：`fuse_focus_masks_F1(m_A, m_C, w_A=0.5, w_C=0.5)`
- 公式：`m_raw = 0.5·m_A + 0.5·m_C` → 三段量化 {0.1, 0.5, 1.0}（thr_fg=0.7, thr_ctx=0.3）
- 在 LIBERO 4 task × 10 frame 上测试输出 mask 的可视化

### W2 D11（周四）· FSF projector 代码

#### 11.1 A 同学：FSF projector 代码

```python
class FSFProjector(nn.Module):
    def __init__(self, student_dim=4096, hidden_dim=2048, teacher_dim=1024, n_patch=256):
        super().__init__()
        self.bn = nn.BatchNorm1d(student_dim)
        self.mlp = nn.Sequential(
            nn.Linear(student_dim, hidden_dim),
            nn.GELU(),
            nn.Linear(hidden_dim, teacher_dim),
        )
        self.register_buffer('pos_enc', build_2d_sinusoidal_pe(n_patch, teacher_dim))
    
    def forward(self, x_student):
        B, N, _ = x_student.shape
        x = self.bn(x_student.reshape(B*N, -1)).reshape(B, N, -1)
        x = self.mlp(x)
        x = x + self.pos_enc[None]
        return x
```

**关键产出**：unit test 通过，forward (1, 256, 4096) → (1, 256, 1024)

#### 11.2 B 同学：Focus mask 阈值与 bg_w 初步调试

- 在 50 张 LIBERO 图上扫描 thr_fg ∈ {0.5, 0.6, 0.7, 0.8} × thr_ctx ∈ {0.2, 0.3, 0.4}
- 目视选择最合理的阈值组合（前景区域应覆盖物体，背景应覆盖桌面/空间）

### W2 D12（周五）· FSF 训练 sanity + 双源 F1 完整跑通

#### 12.1 A 同学：FSF projector 在小 batch 上训练 50 step

- 准备：5 个 LIBERO-Spatial demo（每个 50 frame）
- 离线跑 FastVGGT 拿 teacher feature（共 250 frame × 30ms = 7.5s）
- 训练：β = 0.5，loss 应在 50 step 后下降 ≥ 30%
- **关键产出**：FSF loss 收敛曲线 + projector 不报错

#### 12.2 B 同学：双源 F1 pipeline 全跑

- 在 100 帧上跑完整 pipeline（Source A + Source C + F1 双源融合）
- 输出每帧 14×14 mask 可视化（前景/上下文/背景 3 色叠加在原图上）

### W2 D13（周六）· VGGT 缓存 dry-run + Focus mask 100 帧验证

#### 13.1 A 同学：VGGT 缓存 pipeline 设计

- WebDataset 格式 dry-run（100 frame）
- 估算磁盘空间：50 KB × 100 = 5 MB，全 demo 推算 650 GB
- 验证 fp8 量化的精度损失（应 < 1%）

#### 13.2 B 同学：Focus mask 100 帧目视验证

- 对 100 张 LIBERO 图，目视检查双源融合的前景/上下文/背景三段量化是否合理
- 列出失败案例（如物体被严重遮挡时 Source A 投影偏离、DINOv2 突出干扰物等）
- 输出：可视化 PDF 报告

### W2 D14（周日）· N0 决策会

#### N0 决策会议（共同 2 小时）

议程：
1. **3 仓库装通确认**（OpenVLA-OFT / FastVGGT / LeRobot 真机；**不含 SAM2/GroundedSAM**）
2. FastVGGT 在 LIBERO 上 sanity check：mIoU、depth map 质量
3. DINOv2 CLS attention 提取 + 方向词 GT 投影都能跑
4. FSF projector + Focus mask 双源 pipeline 跑通
5. 真机 1 demo 录制确认

**N0 决策矩阵**：

| 条件 | 决策 |
|---|---|
| ✅ 3 仓库都装通（OpenVLA-OFT + FastVGGT + LeRobot 真机）+ FastVGGT mIoU ≥ 0.5 + Focus mask 双源跑通 + 真机 demo 成功 | **进入 P1** |
| 🟡 FastVGGT mIoU < 0.5（但其他都过） | 切到 VGGT 原版（+1 周 buffer） |
| 🟡 DINOv2 attention 在 LIBERO 上不合理（突出干扰物） | 调阈值；fallback 仅 Source A 单源 |
| 🟡 真机平台不可用 | 切到真机降级（qualitative case study） |
| 🔴 多个仓库装不通 | 切到 **v2 only**（关 FSF） |

## § 4 · 关键交付物 checklist（D14 验收）

A 同学交付：
- [ ] OpenVLA-OFT 环境装通 + Layer 24 hook
- [ ] FastVGGT 装通 + 5 张 LIBERO 图前向 + depth map 可视化
- [ ] VGGT 原版 vs FastVGGT 输出对比
- [ ] LoRA 微调骨架跑通 5 step
- [ ] 真机平台 setup + 1 demo 录制
- [ ] FSF projector 代码 + 单元测试通过
- [ ] FSF training sanity（50 step loss 下降 ≥ 30%）
- [ ] VGGT 缓存 pipeline 设计 + 100 帧 dry-run

B 同学交付：
- [ ] SF 论文摘要（1 页）
- [ ] FocusVLA 中 VGGT 用法摘要（1 页）
- [ ] DINOv2 CLS attention 提取脚本（已在 OpenVLA-OFT vision encoder 中，直接 hook）
- [ ] 方向词 GT 标注脚本（沿用 v2，质检 LIBERO 4 suite × 10 demo）
- [ ] Focus mask Source A（方向词 GT 投影）实现 + 10 张验证
- [ ] Focus mask Source C（DINOv2 attention）实现 + 10 张验证
- [ ] Focus mask 双源 F1 融合 pipeline（0.5·A + 0.5·C） + 100 张目视验证 PDF
- [ ] **不交付** SAM2/GroundedSAM 装通（v2 纯净路径已排除）

共同交付：
- [ ] N0 决策会议纪要
- [ ] 进入 P1 的 confirmed go/no-go

## § 5 · 资源与依赖清单

### 5.1 仓库与代码

**必需仓库（3 个核心）**：

| 仓库 | URL | 用途 |
|---|---|---|
| OpenVLA-OFT | https://github.com/openvla/openvla | 主 backbone |
| FastVGGT | https://github.com/mystorm16/FastVGGT | 3D teacher |
| LeRobot (真机) | https://github.com/huggingface/lerobot | 真机数据采集 |

**参考与数据资源**：

| 资源 | URL | 用途 |
|---|---|---|
| VGGT 原版 | https://github.com/facebookresearch/vggt | fallback teacher（FastVGGT 失败时） |
| Spatial Forcing | https://github.com/OpenHelix-Team/Spatial-Forcing | FSF 方法参考代码 |
| DINOv2 | （已在 OpenVLA-OFT pipeline） | Focus mask Source C 实现源 |
| LIBERO-Plus | https://huggingface.co/datasets/senyufei/LIBERO-Plus | 评测 benchmark |

**不引入（v2 纯净路径）**：
- ~~SAM2 (facebookresearch/sam2)~~：原计划作 Focus mask Source B，已排除
- ~~GroundingDINO (IDEA-Research/GroundingDINO)~~：原计划配合 SAM2，已排除
- ~~GroundedSAM (IDEA-Research/Grounded-Segment-Anything)~~：原计划配合 SAM2，已排除
- 排除理由详见 [[01_主方案_FocusedSpatialForcing]] § 3.1 与 [[07_为什么不选其他方案]]

### 5.2 数据准备

- LIBERO 4 suite（Spatial/Object/Goal/Long）原 demo：~26000 trajectory
- LIBERO-Plus 7 维扰动评测集
- 真机数据：W2 D9 起开始录制（最终 3 任务 × 50 demo = 150）

### 5.3 GPU 资源

- W1-W2 GPU-h 需求：~8 GPU-h（FastVGGT sanity + LoRA 骨架训练）
- 学校共享 GPU 优先；云 GPU（AutoDL）作 backup

## § 6 · 风险与缓解（P0 阶段）

| 风险 | 概率 | 缓解 |
|---|---|---|
| OpenVLA-OFT 在 conda env 装不通 | 25% | 用作者推荐 docker image（HF docs 提供） |
| FastVGGT 与 PyTorch / CUDA 版本冲突 | 35% | 用 FastVGGT 推荐版本（torch 2.1 + CUDA 12.1）；fallback VGGT 原版 |
| DINOv2 CLS attention 在 LIBERO 上突出干扰物 | 20% | 调阈值；fallback 仅 Source A 单源（双源 → 单源） |
| 真机平台不可用或借用受阻 | 30% | 切到真机降级（qualitative case study） |
| 2 大二同学环境配置不熟 | 30% | AI 辅助（Claude/GPT API）+ 师哥/导师 office hour |

## 附录 A · 方向词 GT 标注脚本（继承 v2）

```python
def annotate_directions(demo_trajectory):
    """
    输入：LIBERO demo (hdf5)，含 robot0_eef_pos 时序与 object_pos 时序
    输出：每步的方向词标签 ∈ {right, left, up, down, front, back, grasped}
    """
    direction_labels = []
    EPS_GRASP = 0.03  # 抓取距离阈值 (m)

    for t in range(demo_trajectory.length):
        eef_pos = demo_trajectory['observations/robot0_eef_pos'][t]
        object_pos = demo_trajectory['observations/object_pos'][t]
        gripper_closed = demo_trajectory['observations/gripper'][t] < 0.05

        # 1. 检查 grasped 状态
        dist = np.linalg.norm(eef_pos - object_pos)
        if dist < EPS_GRASP and gripper_closed:
            direction_labels.append('grasped')
            continue

        # 2. 计算方向向量并取主轴
        dir_vec = object_pos - eef_pos
        abs_dir = np.abs(dir_vec)
        main_axis = np.argmax(abs_dir)

        # 3. 映射到 6 方向
        if main_axis == 0:
            label = 'right' if dir_vec[0] > 0 else 'left'
        elif main_axis == 1:
            label = 'front' if dir_vec[1] > 0 else 'back'
        else:
            label = 'up' if dir_vec[2] > 0 else 'down'

        direction_labels.append(label)

    # 4. 滑动平均 3 步去抖
    smoothed = sliding_majority_vote(direction_labels, window=3)
    return smoothed
```

## 附录 B · Focus Mask 双源融合伪代码（v2 纯净版）

```python
def make_focus_mask(image, p_eef, p_obj, K, R, t,
                    dinov2, n_patch=256):
    """
    输入：image, robot/object 3D pose, 相机参数, DINOv2 model
    输出：(256,) focus mask ∈ [0.1, 1.0]^256（三段量化）
    
    设计原则：v2 纯净路径——仅使用 v2 内工具
      - Source A: 方向词 GT 倒推 2D Gaussian（继承 v2 投影工具）
      - Source C: DINOv2 CLS-to-patch attention（已在 OpenVLA-OFT vision encoder 中）
      - 不引入 Source B (SAM2/GroundedSAM)
    """
    # Source A: 方向词 GT 倒推 2D Gaussian
    m_A = focus_mask_source_A(p_eef, p_obj, K, R, t)
    
    # Source C: DINOv2 CLS attention
    m_C = focus_mask_source_C(image, dinov2)
    
    # F1 双源融合 + 三段量化
    m_raw = 0.5 * m_A + 0.5 * m_C
    m = np.where(m_raw >= 0.7, 1.0,
        np.where(m_raw >= 0.3, 0.5, 0.1))
    return m
```

## 附录 C · 期望的 D14 N0 决策模板

填写时间：2026-06-XX

| 检查项 | 状态 | 备注 |
|---|---|---|
| OpenVLA-OFT 装通 | ✅/❌ | … |
| FastVGGT 装通 | ✅/❌ | … |
| LeRobot 真机平台装通 | ✅/❌ | … |
| FastVGGT 在 LIBERO 上 mIoU | _._._ | 目标 ≥ 0.5 |
| DINOv2 CLS attention 提取脚本 | ✅/❌ | … |
| 方向词 GT 标注脚本（v2 复用） | ✅/❌ | … |
| Focus mask 双源 pipeline（Source A + Source C） | ✅/❌ | … |
| FSF projector 代码 + 训练 50 step sanity | ✅/❌ | loss 下降 ≥ 30%？ |
| 真机 1 demo 录制 | ✅/❌ | … |
| **总决策** | **进 P1 / 切 v2 only / 切 Plan B-X** | 签字：A / B / 师哥 |

%% Updated: 2026-05-27 %%
