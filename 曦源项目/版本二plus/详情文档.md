<!--
本文档由 2026-05-27 阶段 3 重构产出。来源映射：
- §1 项目摘要与立项依据：合并自 legacy/立项调研/00_总览与立项决定.md §1-3
  与 legacy/立项调研/09_与版本二差异化说明.md §1-2。删除 v1 / v3 备选轨道历史
  与"师哥建议引述"原话（保留决策结论）。
- §2 文献综述与定位：合并自 legacy/立项调研/06_关联资料索引.md §1-2 与
  legacy/研究报告/06_3D-aware_VLA_相关工作综述.md 选段。差异化论证按
  revision_plan.md §1.2 方向 B' 重写——删除 legacy/研究报告/03 中 3 段虚构
  引文（§2.1/§2.2/§2.3）；引文 4 章节标注更正为论文 §4.3 Ablation Study；
  FocusVLA 数据按 arXiv:2603.28740 原文重写（with VGGT only 96.8% 低于
  baseline 97.1%；最佳 98.7% 不用 VGGT）。所有论文作者补正至原文（修复
  legacy 索引中至少 11 处 "Anonymous" 误标）。
- §3 方法学：合并自 legacy/立项调研/01_主方案_FocusedSpatialForcing.md §1-4
  与 legacy/研究报告/01_Spatial_Forcing_深度精读.md §2-4 与
  legacy/研究报告/04_Focus_Mask_设计调研.md §1-3。Focus mask 直接定义 A+C
  双源（删除 SAM2 / Source B 排除决策档案）。Teacher 锁 FastVGGT 单一选项。
- §4 评测设计：合并自 legacy/立项调研/01 §7 与 legacy/研究报告/05_真机平台
  与数据标注方案.md §6。统一真机评测为 12 cell（3 任务 ×(3 扰动 +1 clean)）
  与 360 episode 评测 / 150 demo 采集。H2 改为 V3 > V2 + 1.5pp；H4 阈值
  从 ≥5pp 降到 ≥3pp，p<0.10，含"非事后调整"辩护语句。
- §5 工程规划：合并自 legacy/立项调研/03 §P0 与 legacy/立项调研/04 §2-§5。
  GPU-h 统一为紧约束 230。Ablation 砍至 6 组（A1-A6；A7/A8 / Teacher 备
  选项不再列入）。
- §6 风险与缓解：保留 legacy/立项调研/05 Top 5 风险，删 LibraryCode 环境
  等 P3 项。真机平台风险作模糊处理（不指型号）。
- §7 参考文献：核查通过的 22 篇，删除占位符 arXiv ID 引用（如 Gaze-Reg
  / AutoFocus-IL 等）。
-->

# FSF-VLA 项目详情文档

## §1 项目摘要与立项依据

### 1.1 项目代号与一句话定义

项目代号 FSF-VLA（Focused Spatial Forcing for Vision-Language-Action Models）。在 OpenVLA-OFT [1] 主 backbone 之上沿用 Spatial Forcing [2] 提出的 LLM Layer 24 中间层 VGGT 蒸馏范式，新增一个由方向词 ground truth 与 DINOv2 CLS attention 双源融合的 focus mask 调制 align loss，并叠加 InSpire [3] 风格的显式方向词提示。Teacher 锁 FastVGGT [4] 单一选项做离线缓存；评测同时覆盖 LIBERO-Plus [5] 7 维仿真扰动与小规模真机扰动两条轴。

### 1.2 问题与立项依据

视觉-语言-动作（VLA）模型在 LIBERO 等标准基准上已达 95% 以上的成功率，但 LIBERO-Plus [5] 在 7 个扰动维度（物体布局、相机视角、机器人初始状态、语言指令、光照、背景纹理、传感器噪声）上的系统评测显示，主流 VLA 模型成功率会从 95% 跌至 30% 以下。这一断崖式下跌是 VLA 走出实验室的关键障碍。

文献中针对扰动鲁棒性主要存在两条互补的路径。一条是 InSpire [3] 风格的"显式空间提示"——在原任务前置一个方向性问题，让模型先输出方向词再生成动作，从输出层引入与空间关系直接耦合的中间表示。另一条是 Spatial Forcing [2] 风格的"隐式 3D 表征对齐"——以冻结的 VGGT [6] 为 teacher，对 VLA 视觉编码器中间层做 per-token 余弦相似度对齐，让 VLA 在不引入额外 3D 输入的前提下隐式获得空间感知。这两条路径各自在原论文上得到验证，但它们的协同效应、以及在统一的多扰动评测与真机扰动两轴下的系统验证，至今仍是空白点。

本项目的科研价值由三个空白点界定：第一，InSpire 显式方向词提示与 Spatial Forcing 隐式 3D 对齐的叠加效应缺乏直接实证；第二，Spatial Forcing 对所有 visual token 采用均匀对齐权重，未充分利用任务相关性先验，而双源融合 focus mask（方向词 ground truth 倒推 2D Gaussian + DINOv2 CLS attention）调制 align loss 在已发表工作中尚未见到；第三，仿真高分与真机扰动表现之间的差距在公开数据集上缺乏小规模本科生可执行的对照评测样本。

### 1.3 立项约束

复旦曦源项目 1 年期、2 人本科生、¥10000 预算。指导教师所在实验室具备 6 自由度以上机械臂、第三视角相机与工作台标定板，可支撑本项目真机数据采集与扰动评测。云端 GPU 算力按紧约束版规划约 230 GPU 小时，目标产出 4-6 页 workshop short paper 投稿至 CoRL 2027 / ICLR 2027 / NeurIPS 2027 Robot Learning workshop，并公开发布代码、模型权重与真机数据集。

---

## §2 文献综述与定位

### 2.1 VLA 主 backbone 与 OpenVLA-OFT 选择

OpenVLA-OFT [1] 在 OpenVLA 原版基础上引入了并行解码、连续动作表示与 chunked action head，把 LIBERO 4 个 task suite 的平均成功率从 76.5% 提升到 97.1%，并对 LoRA 微调友好。本项目以 OpenVLA-OFT 为统一主 backbone，所有变体共享相同的 visual encoder（SigLIP + DINOv2）、相同的 Llama2-7B LLM、相同的 LoRA 配置（r=16，target_modules 为 q_proj/k_proj/v_proj/o_proj），仅在显式提示插件、focus mask 调制 align loss 与训练数据三个维度上做对照。

### 2.2 隐式 3D 表征对齐：Spatial Forcing 范式

Spatial Forcing [2] 由 OpenHelix Team（Li 等，2025-10 投稿 ICLR 2026）提出。其核心做法是：以冻结的 VGGT [6] 为 teacher，对 VLA 主干 Llama2-7B Layer 24 抽取 visual token hidden state，过 BatchNorm + 2 层 MLP + 2D sinusoidal positional encoding 投影到 1024 维，然后与 VGGT aggregator Layer 23（倒数第二层）的 patch token 做 per-token 余弦相似度对齐：

$$\mathcal{L}_{\text{align}} = -\frac{1}{N}\sum_{i=1}^{N} \cos\Big(\mathrm{MLP}\!\cdot\!\mathrm{BN}(x_i^{V,L24}) + E_i,\; f_i^{3D,L23}\Big)$$

最终训练目标为 $\mathcal{L}_{\text{SF}} = \mathcal{L}_{\text{action}} + 0.5 \cdot \mathcal{L}_{\text{align}}$。Spatial Forcing 论文在 LIBERO 上取得 98.5% 平均成功率（vs OpenVLA-OFT baseline 97.1%，长程任务 +3.8pp），训练加速 3.8 倍，达到 baseline 95% 性能所需训练数据降至 17%，并在 RoboTwin 真机评测上相对 baseline 提升 16.6pp。该方法仅约 30 行核心代码改动，对原 VLA 推理路径零侵入。

Layer 24（在 32 层 Llama2-7B 中位于约 75% 深度）的选择是经过 Spatial Forcing 原论文 §4.4 完整层位扫描确定的最优值。过浅的层处于 language-vision token mixing 阶段尚未形成可对齐的空间语义，过深的层已被压缩到 action decoding，强行注入 3D 监督会破坏 task understanding。Teacher 取 VGGT aggregator 倒数第二层（Layer 23）而非最后一层，是因为最后一层 feature 已被各个 task-specific head 拉向特定输出，而倒数第二层保留了最丰富的通用 3D 语义。

### 2.3 显式方向词提示：InSpire 范式

InSpire [3] 由 Zhang 等（UESTC，2025-05，arXiv:2505.13888）提出。其做法是在原 VLA 任务的语言 instruction 前置一个方向性问题（"In which direction is the [object] relative to the robot?"），要求模型先输出方向词（right/left/up/down/front/back/grasped 共 7 类），再生成 action。方向词作为输出层的离散中间监督，强制模型显式建立"指令-空间关系"的耦合。InSpire 在原 LIBERO 上提升有限，但在视觉扰动维度上表现出鲁棒性增益。

### 2.4 VGGT 与 FastVGGT teacher 选择

VGGT [6] 是 CVPR 2025 论文，由 Meta FAIR 与 Oxford VGG 联合发布，作者 Wang/Chen/Karaev/Vedaldi/Rupprecht/Novotny。其核心创新是在单一 transformer + 单次 forward 中推理多种 3D 量（相机参数、深度图、点图、轨迹），参数量 1.2B。VGGT 在 16 个 3D 数据集（Co3Dv2, BlendMVS, MegaDepth, Kubric, ScanNet, HyperSim 等）上预训练，feature 同时编码深度、相机姿态、表面法向与点云对应关系。

FastVGGT [4]（You Shen 等，arXiv:2509.02560）通过 training-free 的 token merging 把 VGGT 推理加速 4 倍，深度 δ<1.25 精度损失约 0.4pp，训练显存降 29%。该实现与 VGGT 原版 100% 接口兼容，加载 VGGT 原 checkpoint 后运行时切换 merge ratio 即可。本项目锁 FastVGGT 作为唯一 teacher 选项，不再保留 StreamVGGT / HD-VGGT / π³ 等续作作为备选，以工程稳定性与本科生可执行性优先。

### 2.5 与 FocusVLA 的差异化定位

FocusVLA [7]（Zhang Yichi 等，arXiv:2603.28740）是同时期与本项目主题相近的工作，提出 Modality Cascaded Attention 与 Focus Attention 机制以缓解 VLA 视觉利用问题。FocusVLA 也使用了 VGGT，但其用法与 Spatial Forcing 存在结构性差异——FocusVLA 把 VGGT 视为与 SigLIP / DINOv2 并行的第三个视觉 encoder，在 policy 阶段（VLM 完成所有 self-attention 之后）由 Focus Attention 的 top-K + channel gate 间接利用其 token；VGGT 与 VLM 主干之间没有任何梯度交互。

本项目相对 FocusVLA 的差异化论证有一个直接可引证的反转点：FocusVLA 论文 Table 2 显示，"with VGGT only" 配置在 LIBERO 上平均成功率为 96.8%，反而比 OpenVLA-OFT baseline 97.1% 低 0.3pp；而 FocusVLA 最佳成绩 98.7%（multi-weights setting）采用的是 VLM+DS+VLM 配置（DINOv2+SigLIP+VLM），主动舍弃 VGGT。FocusVLA 论文 §4.3 Ablation Study 中作者明确写道："Due to architectural constraints, to avoid disrupting the pretrained VLM, VGGT features can only be injected at the policy stage. As a result, their gradients are relatively weaker and less stable, which limits their performance upper bound."（论文原文）。这是 v2plus 选择继承 Spatial Forcing 中间层 teacher 对齐范式、不采纳 FocusVLA policy 阶段并联范式的直接证据：FocusVLA 在自家数据中证伪了 VGGT policy 并联的有效性，而 Spatial Forcing 的同表 SR 为 98.5%，证明 VGGT 在中间层做 teacher 对齐是有效路径。

关键差异有两点。第一，VGGT 接入位置不同：本项目沿用 Spatial Forcing 在 LLM Layer 24 做中间层蒸馏（直接反传梯度至 Layer 1-24），而 FocusVLA 在 policy 阶段并联（VGGT 与 VLM 主干无梯度交互）。第二，"focus" 概念作用位置不同：FocusVLA 的 focus 作用于 attention 计算（top-K 视觉 token 筛选 + channel-wise gate），目的是减少推理时的 token 数；本项目的 focus 作用于 align loss 的 per-token 监督权重（双源融合 mask 调制三段量化），目的是让 3D 监督信号更专注于任务相关区域、不被背景稀释。

### 2.6 Focus mask 与 attention guidance 相关工作

近期已有若干工作探索用 saliency mask 引导 VLA 训练。AttentionVoxel [8]（Yurchyk 等，Humanoids 2025）把 DINOv2 CLS attention 提升到 3D voxel grid 作为操作 saliency 先验，与本项目的 Source C 同源但作用空间不同（3D voxel vs 2D patch loss 加权）。同期已发表与未发表工作的探索范围覆盖 VLM-based saliency 替代人类 gaze、人类眼动监督 attention 正则、以及历史视觉轨迹作为输入端 prompt 等多种思路。本项目相对这些工作的独特定位有三点：将 saliency mask 用于调制 VGGT-VLA 蒸馏对齐 loss 而非 attention 计算；方向词 ground truth 倒推 2D Gaussian 与 DINOv2 CLS attention 双源融合；三段量化（前景 1.0 / 上下文 0.5 / 背景 0.1）的显式权重设计。

### 2.7 鲁棒性评测与失败检测

LIBERO-Plus [5]（Fei 等，arXiv:2510.13626）在原 LIBERO [9] 4 个 task suite 与 130 个任务基础上，引入 7 个扰动维度的系统评测集，是当前最完整的 VLA 仿真鲁棒性 benchmark。SAFE [10]（Qiao Gu 等，NeurIPS 2025）提出基于 VLA 内部 feature 的多任务 failure detection 框架，是本项目 H3 假设（运行时失败检测）的直接 baseline。Averaging Trap [11]（Tang 等，arXiv:2603.18342）指出 entropy 平均聚合会稀释短促但安全关键的不确定性 spike，本项目用 InSpire 离散方向词预测的时序切换作为失败信号正是该问题的一种解药。

### 2.8 3D-aware VLA 工作分类

3D-aware VLA 工作大致分为五类：直接 RGB-D 输入路线（如 SpatialVLA [12]、3D-Mix [13]、AugVLA-3D [14]、DepthVLA [15]），显式点云路线（如 PointVLA），单目深度估计辅助路线（如 DepthVLA），多视图重建路线（基于 DUSt3R / MASt3R），以及隐式 3D 对齐路线（Spatial Forcing [2]、本项目 FSF-VLA）。本项目属于隐式对齐路线，与显式 3D 输入路线的差异是推理路径零侵入与零额外 latency。

---

## §3 方法学：FSF-VLA 详解

### 3.1 整体架构

FSF-VLA 在 OpenVLA-OFT 主 backbone 之上沿用 Spatial Forcing 的训练时 VGGT teacher 蒸馏架构，新增三个组件：FastVGGT 离线缓存 pipeline、focus mask 双源生成 pipeline、focus mask 调制 align loss。推理时所有新增组件被丢弃，仅运行 student VLA，因此推理 latency 与 OpenVLA-OFT 完全相同。

训练流程：输入 RGB（224×224）经 SigLIP + DINOv2 patch embedding 后进入 Llama2-7B Layer 1-32 前向；在 Layer 24 抽取 visual token hidden state（256 个 patch token × 4096 维），过 BN + 2 层 MLP（4096 → 2048 → 1024）+ 2D sinusoidal PE 得到 student 表征。同帧 RGB 通过冻结的 FastVGGT 离线缓存得到 aggregator Layer 23 输出（256 × 1024 维）作为 teacher 表征。Focus mask 由方向词 ground truth 倒推 2D Gaussian（Source A）与 DINOv2 CLS attention（Source C）双源融合后三段量化得到。Action head 输出 7 自由度 chunked action（chunk size H=8）。

总损失：

$$\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{action}} + \alpha \cdot \mathcal{L}_{\text{direction}} + \beta \cdot \mathcal{L}_{\text{FSF}}$$

其中 $\alpha = 0.1$ 沿用 InSpire 风格平衡，$\beta = 0.5$ 沿用 Spatial Forcing 原论文最优 α=0.5 的位置。$\mathcal{L}_{\text{action}}$ 是 OpenVLA-OFT 原始的 L1 chunked action regression loss，$\mathcal{L}_{\text{direction}}$ 是 7-class 方向词交叉熵 loss。

### 3.2 FSF align loss 的精确形式

FSF align loss 在 Spatial Forcing 原版均匀监督基础上引入 per-token 权重 $m_i \in \{0.1, 0.5, 1.0\}$：

$$\mathcal{L}_{\text{FSF}} = -\frac{1}{\sum_{i=1}^{N} m_i}\sum_{i=1}^{N} m_i \cdot \cos\Big(\mathrm{MLP}\!\cdot\!\mathrm{BN}(x_i^{V,L24}) + E_i,\; f_i^{3D,L23}\Big)$$

分母 $\sum m_i$ 归一化保证不同 mask 分布下 loss 整体尺度一致。这是 Spatial Forcing 原版 $\frac{1}{N}$（均匀）的自然推广——当所有 $m_i = 1$ 时退化为 Spatial Forcing 原版。

### 3.3 Focus mask 双源融合

Focus mask 由两个互补源融合生成。

Source A 是方向词 ground truth 倒推 2D Gaussian。从 LIBERO demo 或真机数据的 robot end-effector 3D 位姿与目标物体 3D 位姿出发，通过相机内外参投影到 2D 图像坐标 $(u_{\text{eef}}, v_{\text{eef}})$ 与 $(u_{\text{obj}}, v_{\text{obj}})$，在两处分别构造 2D Gaussian（$\sigma_{\text{eef}}=30$ 像素，$\sigma_{\text{obj}}=40$ 像素），取像素级 max 后下采样到 14×14 patch grid，得到 $m^A \in [0, 1]^{256}$。Source A 提供任务相关性先验，定位精确但仅覆盖两个中心点，对非凸物体形状不敏感。

Source C 是 DINOv2 CLS attention。DINOv2 已在 OpenVLA-OFT vision encoder 中作为 SigLIP 的并行 encoder 存在，无需额外推理。从 DINOv2 最后一层 self-attention 中取 CLS-to-patch attention（多头平均），min-max 归一化到 [0, 1] 即得 $m^C \in [0, 1]^{256}$。Source C 提供模型自身的视觉显著性先验，形状互补但不一定与任务对齐（可能突出装饰物或干扰物）。

融合采用 F1 加权平均策略：

$$m_i = \text{quantize}\big(\text{clip}(0.5 m_i^A + 0.5 m_i^C,\ 0.1,\ 1.0)\big)$$

其中量化阈值为：$m_{\text{raw}} \geq 0.7 \to 1.0$（前景），$0.3 \leq m_{\text{raw}} < 0.7 \to 0.5$（上下文），$m_{\text{raw}} < 0.3 \to 0.1$（背景）。三段量化的直接语义对应是"前景应被强监督、上下文应被中等监督、不相关背景不应完全忽略但应被弱监督"——bg=0.1 而非 bg=0 是因为硬 mask（bg=0）会让背景区域完全丢失梯度信号，反而损害 VLA 主干对背景的几何理解；软 mask（bg=0.1）保留 10% 信号，对 Source A/C 误判的鲁棒性更好。

### 3.4 三个变体与 ablation 设计

主对照实验设 3 个变体：V1 vanilla（OpenVLA-OFT + LoRA r=16，原 LIBERO instruction，仅 $\mathcal{L}_{\text{action}}$）、V2 +InSpire（V1 + 前置方向词 prompt + 7-class direction head + $\alpha \cdot \mathcal{L}_{\text{direction}}$）、V3 +InSpire+FSF（V2 + FastVGGT teacher + 双源 focus mask + $\beta \cdot \mathcal{L}_{\text{FSF}}$）。V3 是本项目主创新点，V2 是 v2 版本的标杆，V1 是 OpenVLA-OFT baseline 校准。

Ablation 设计共 6 组：

| 编号 | 内容 | 目的 | GPU 小时 |
|---|---|---|---|
| A1 | VGGT teacher 取层 {Layer 12, 18, 23} | 验证 Layer 23 最优 | 15 |
| A2 | Student 监督层位 {Layer 8, 16, 24, 32} | 验证 Layer 24 最优 | 20 |
| A3 | Mask 来源 {M-None, M-A, M-C, M-AC, M-Random} | H5 核心：双源 vs 单源 vs 均匀 | 35 |
| A4 | β 粗扫 {0.1, 0.3, 0.5, 0.8} | 验证 β=0.5 最优 | 10 |
| A5 | 背景权重粗扫 {0, 0.05, 0.1, 0.3} | 验证 bg=0.1 最优 | 10 |
| A6 | 视觉利用机制分析（token dropout + attention 可视化）| 机制层证据 | 25 |

合计 115 GPU 小时。设计原则是从 v2plus 设计稿原 8 组中砍掉融合策略扫描（F2/F3/F4）与方向词粒度扫描（6 类/12 类），以及 teacher 备选项扫描（StreamVGGT 等），匹配紧约束 GPU 预算。

### 3.5 FastVGGT 离线缓存

LIBERO 4 个 task suite 共约 26000 条 demo，平均每条 50 帧，合计约 1.3M frame。若每个 epoch 重新跑 FastVGGT 前向，仅 teacher 计算需消耗约 1100 GPU 小时（按 100 epoch 计），完全超出预算。采用一次性离线预提取：一次性消耗约 11 GPU 小时跑完所有 frame 的 FastVGGT aggregator Layer 23 feature，缓存到本地 SSD。

缓存格式采用 WebDataset（tar shards），256×1024 维 bf16 feature 单帧 0.5 MB，1.3M frame 合计约 650 GB。如学校共享存储不足，启用 fp8 量化将占用降到约 325 GB，精度损失小于 1%。如仍不足，可仅缓存 LIBERO-Spatial 单个 suite（约 81 GB）先验证 FSF 收敛性。备选方案是 streaming VGGT（训练时在线计算），代价是 GPU 小时增加 3-5 倍。

### 3.6 LoRA 微调配置

target_modules 设为 q_proj / k_proj / v_proj / o_proj，r=16，alpha=32，dropout=0.05。单卡 A100 80GB 显存占用：VLA forward+backward 约 32 GB，FastVGGT 缓存加载约 1 GB（vs 在线计算 11 GB），projector 与 optimizer state 约 5 GB，DINOv2 attention hook 约 2 GB，合计约 40 GB，留有余量给数据 augmentation 与 batch 扩展。

每个 variant × 每个 seed 训练 50K step（OpenVLA-OFT 默认），单 seed 8-12 小时。

---

## §4 评测设计

### 4.1 仿真评测：LIBERO-Plus 7 维

主仿真评测采用 LIBERO-Plus 的 7 维扰动评测集（物体布局 / 相机视角 / 机器人初始状态 / 语言指令 / 光照 / 背景纹理 / 传感器噪声）。每维度对每个变体跑 300 instance / cell × 3 seed，共 21 cell（7 维 × 3 变体），合计 21 × 300 × 3 = 18900 episode。单 episode 平均推理 30 秒，总 GPU 小时约 158。

干净基线（clean LIBERO 4 task suite）作为校准，每变体跑 3 seed × 130 task × 50 instance ≈ 19500 episode，但与 LIBERO-Plus 维度评测共享部分计算，实际增量 GPU 小时约 20。

统计检验：每对变体差异用 paired t-test（同 seed × 同 instance 配对），7 维度上做 Bonferroni 多重比较修正，单维度有效显著性阈值 α = 0.05 / 7 ≈ 0.007。

### 4.2 真机评测：3 任务 × 12 cell

真机评测在实验室已有的 6 自由度以上机械臂平台上进行，第三视角相机固定。3 个真机任务覆盖不同难度：T1 block-to-bowl（基础抓放，预期 clean SR 80-95%）、T2 push-block-to-target-line（中等推动力控制，无 gripper 闭合，预期 60-80%）、T3 mug-to-plate-with-occluder（有干扰物的部分遮挡场景，预期 40-70%）。

训练采集：每任务 50 demo，共 150 demo。在 LIBERO 训好的 LoRA 基础上做小规模 calibration fine-tune（10-20 step），不全调，主要校准摄像头视角与物体外观分布差异。

评测设计 12 cell = 3 任务 ×（3 扰动 + 1 clean）。3 个扰动维度与 LIBERO-Plus 部分对应：D1 光照（暗光 / 侧射强光 / 频闪选 3 个子条件）、D2 背景纹理（木纹 / 杂乱 / 反光选 3 个子条件）、D3 初始位置（5×5 cm grid 9 个起点中选 3 个）。每 cell 跑 3 变体 × 30 episode，物理 reset 在 3 变体之间共享，合计物理执行约 12 × 30 = 360 episode 的 reset 工作量。

单 episode 时长：reset 20s + 推理 30s + 评估 10s 约 1 分钟。整 cell 30 分钟纯执行 + 10 分钟调试。12 cell 累计约 8 小时纯执行 + 调试 + 数据落地，分布在 P3 阶段约 5 天内完成。

### 4.3 5 个 H 假设的形式化

**H1（仿真鲁棒性增益）**：在 LIBERO-Plus 7 维扰动评测中，V3 (+InSpire+FSF) 相对 V1 (vanilla) 在至少 2 个扰动维度上 SR drop 显著减小 ≥ 5pp（paired t-test, p < 0.05, Bonferroni 修正）。基于先验机制分析，最可能显著的维度是 viewpoint 与 robot initial state（与 3D 表征对齐对几何变化敏感的预期一致），最不可能显著的是 language instruction（LIBERO-Plus 论文已报该维度对所有 VLA 都不显著）。预期 V3 在 viewpoint 维度上相对 V1 提升 6-10pp（InSpire 贡献 3-5pp + FSF 贡献 3-5pp 叠加）。

**H2（叠加效应）**：V3 平均 SR 显著高于 V2 (+InSpire) 至少 1.5pp（paired t-test, p < 0.05）。这一阈值匹配 Spatial Forcing 原论文相对 OpenVLA-OFT baseline 的提升幅度（+1.4pp 平均、+3.8pp 长程）。H2 与 H1 的关系是：H1 验证 V3 vs V1 的整体增益，H2 单独验证 FSF 在已有 InSpire 基础上的边际贡献。

**H3（Failure Predictor）**：用 V2 或 V3 的方向词预测置信度时序训练 LightGBM 二分类器，预测同 episode 最终成败 AUC ≥ 0.65（5-fold cross validation）。这是 v2 已验证的失败检测假设，本项目继承并预期 V3 因 FSF 训练让 hidden state 更 3D-aware 而进一步提升。

**H4（真机鲁棒性增益）**：V3 在真机 12 cell 平均扰动 SR drop 显著比 V1 小 ≥ 3pp（paired t-test, p < 0.10）。

**H4 阈值辩护**：3pp 阈值（与 H1 仿真 5pp 阈值的差异）在 12 cell × 30 episode/cell 的样本规模下对应统计 power 0.7-0.8，是与真机样本 power 匹配的合理阈值，非事后调整——本项目立项阶段即按此样本规模反推确定阈值。H1 仿真之所以用 5pp 是因为单 cell 有 300 instance × 3 seed = 900 episode 的样本规模，可以支撑更严苛的 effect size 阈值；真机 episode 数受人工预算约束（不可能像仿真一样跑 300 instance/cell），按相同 power 反推阈值需要降到 3pp。p 阈值放宽到 0.10 同样来自真机样本规模约束，并非降低显著性标准——同等 effect size 下真机的 p 值天然比仿真高。 [TODO-团队人工核查：请查 InSpire 论文 arXiv:2505.13888 真机实验 table（论文 §4.2 或附录），确认其报告的真机 SR 提升 effect size 区间。若区间覆盖 3pp，则在此处加一句 "3pp 阈值与 InSpire 原论文真机评测报告的典型 effect size 区间（X-Ypp）一致" 作为外部锚点；若区间不覆盖 3pp，则删除本 TODO 不加外部锚点。]

**H5（双源 focus 优于单源/均匀）**：在 A3 mask 来源 ablation 中，M-AC（双源融合）平均 SR 满足两条不等式：M-AC > max(M-A, M-C) + 1pp，且 M-AC > M-None（Spatial Forcing 原版均匀监督）+ 1.5pp（p < 0.05）。同时 M-AC > M-Random + 3pp 验证"focus 位置正确"而非"mask 多样性"是有效来源。H5 是 v2plus 相对 Spatial Forcing 原论文最直接的方法学贡献假设。

### 4.4 多重比较修正与 power 估算

H1 与 H4 均涉及多维度检验，采用 Bonferroni 修正。H1 在 7 维度上单维度 α = 0.007；H4 在 4 扰动条件（3 扰动 + 1 clean baseline）上单条件 α = 0.025。H2 与 H3 单一比较，不修正。H5 在 5 种 mask 配置上的 4 对比较，修正后 α = 0.0125。

statistical power 估算：H1 单 cell 300 instance × 3 seed = 900 episode，paired-difference 5pp 阈值的 power 约 0.85；H4 单 cell 30 episode，paired-difference 3pp 阈值的 power 约 0.7-0.8。H2 与 H3 在 26000 LIBERO demo × 3 seed 的样本规模下 power 接近 1。

---

## §5 工程规划与里程碑

### 5.1 6 阶段时间线与决策节点

项目按 12 个月分为 6 个 phase：P0 环境与真机准备（M0，2 周）；P1 复现与 sanity（M1-M3，3 个月）；P2 仿真主评测与真机数据采集（M4-M6，3 个月）；P3 机制分析与真机评测（M7-M9，3 个月）；P4 failure predictor 与论文撰写起步（M10-M11，2 个月）；P5 论文冲刺与投稿（M12，1 个月）。

每 phase 末设置一个决策节点（N0-N4）。N0 验收 3 个核心仓库（OpenVLA-OFT、FastVGGT、LeRobot 真机 SDK）装通 + FastVGGT 在 LIBERO 渲染图上 depth 质量验证 + DINOv2 attention 提取脚本 + 方向词 ground truth 投影 + 真机 1 demo 录制成功。N1（M3 末）验收 V3 clean SR 相对 V1 不退化 > 3pp、VGGT feature 离线缓存完整。N2（M6 末）验收 H1 至少 2 个维度显著、H5 M-AC > M-None。N3（M9 末）验收 H4 真机增益 ≥ 3pp。N4（M11 末）验收 H3 AUC ≥ 0.65 与论文初稿数据齐全。

任一 Go/No-Go 决策点 No-Go 时，启动 fallback 路径，最坏退到 v2 only（关闭 FSF 通道，仅运行 V1 + V2 方案）。fallback 路径与 v2 共享 OpenVLA-OFT + LoRA + InSpire plugin + LIBERO-Plus 评测脚本的全部代码框架，切换成本不超过 1 周。

### 5.2 团队分工

2 人本科生配置：A 同学工程主导（OpenVLA-OFT + FastVGGT 集成、LoRA 训练 pipeline、VGGT 离线缓存、真机平台搭建），工程占比约 75%；B 同学评测主导（方向词 ground truth 标注质检、Focus mask 双源生成 pipeline、LIBERO-Plus 评测调度与统计、真机数据标注、论文写作），工程占比约 35%。每周固定时段（建议周末 1 天）作为团队同步会议。

每人时间投入约 18 小时/周，54 周累计约每人 970 小时。P3 是密集期，buffer 0.5 个月。月度与指导教师 office hour，phase 末与师哥 + 导师 review 会。

### 5.3 GPU 与预算

紧约束 GPU 小时分配：P0 sanity 8 小时；P1 一次性 VGGT 离线缓存 11 小时 + V1/V2/V3 各 3 seed 训练共约 95 小时；P2 LIBERO-Plus 主评测约 158 小时 + 核心 ablation A1/A2/A3 约 70 小时；P3 视觉机制 ablation A6 + β/bg_w 粗扫 A4/A5 约 45 小时 + 真机 fine-tune 20 小时；P5 reviewer 重跑 8 小时；合计约 230 GPU 小时。学校共享 GPU 优先使用，云 GPU（AutoDL A100 80GB 约 ¥26/小时）作峰值补充与紧急 buffer。

¥10000 预算分 6 项：云端 GPU 算力 ¥6000（约 230 GPU 小时云端补峰，与紧约束 GPU 预算自洽）；学术会议注册与差旅 ¥1500；论文版面费与开放获取 ¥1000；API 与软件订阅 ¥800（代码辅助 API、HuggingFace Pro、Weights & Biases 等）；文献检索与电子资源 ¥300；实验耗材与文印 ¥400（数据存储 SSD、网络流量、文印、真机评测辅助耗材如标定板与背景布）。

真机硬件由指导教师所在实验室提供（实验室已有 6 自由度以上机械臂与第三视角相机），不计入项目预算。

---

## §6 风险与缓解

### 6.1 R1 · FSF Loss 在 OpenVLA-OFT 上不收敛或退化 SR

严重度高，概率约 30%。触发条件是 M3 末 V3 vs V1 clean SR 退化超过 10pp，或 FSF loss 在 1000 step 后未下降 50%，或训练 loss 出现 NaN。缓解措施按优先级：P0 末在 5 个 LIBERO-Spatial demo 上跑 100 step FSF 训练做预先验证（loss 应下降至少 30%）；β 系数从 0.5 起步，必要时按 2× 增量降到 0.1 再到 0.05；逐层冻结策略（FSF 训练时先冻结所有 LoRA 仅训 projector，再解冻 LoRA）；warmup 策略（前 1000 step β=0，后线性 ramp 到 β=0.5）；最坏 fallback 切到 V2 only。

### 6.2 R2 · VGGT 离线缓存超磁盘或缓存失败

严重度高，概率约 30%。触发条件是学校共享存储不足 650 GB，或 WebDataset 多进程 IO 瓶颈，或 FastVGGT 全量预提取 OOM。缓解措施：fp8 量化（650 GB 降到 325 GB，精度损失 < 1%，P0 末验证）；仅缓存 LIBERO-Spatial 1 个 suite（81 GB）先验证 FSF；学校大存储节点或阿里云 OSS 冷存储（约 ¥30/月/100GB，1 年 ¥360 在 ¥400 实验耗材预算内）；streaming VGGT 训练时在线推理（GPU 小时 ×3-5 但占用磁盘 0）。

### 6.3 R3 · Focus mask 双源融合无显著差异（H5 失败）

严重度中，概率约 40%。触发条件是 A3 ablation 显示 M-AC 与最佳单源差异 < 0.5pp，或 M-AC 与 M-None 差异 < 0.5pp。缓解措施：扫描融合策略与阈值找 sweet spot；改叙事降级（"任意 focus 都比 uniform 好"仍是有意义结论）；主提交改单源（M-A 与 v2 工具复用度 100%）；如时间允许 stretch ablation 引入额外 mask 来源验证。H5 的 fallback 不影响 H1 / H3 / H4 的成立。

### 6.4 R4 · 真机数据不足以支撑 fine-tune

严重度中，概率约 45%。触发条件是 M8 真机 V1 zero-shot SR < 30%，或 V3 fine-tune 后 zero-shot SR < V1 - 10pp。缓解措施：调整为仅 calibration fine-tune（10-20 step，不全调）；扩到每任务 100 demo（增加约 5 天人工）；仅 zero-shot 真机评测不做 fine-tune（LIBERO LoRA 直接迁移）；真机降级为 qualitative case study（10 episode/任务）；极端情况减少任务数到 1-2 个。

### 6.5 R5 · 真机平台具体可用性

严重度中，概率约 25%。立项阶段实验室已确认具备 6 自由度以上机械臂、第三视角相机与工作台等基础硬件，但具体型号、可借时段、SDK 兼容性的细节需在 P0 第一周完成确认。如型号是 LeRobot 主线原生支持型号（SO-100 / Koch / Reachy2 等），按 12-13 天真机人工预算执行。如型号需要团队自实现 SDK adapter（如某些 Franka / xArm / AgileX 配置），P0 阶段额外 1-2 周工程量，P1 时段相应弹性扩展 0.5 个月。如 P0 末确认平台不可用，真机降级为 qualitative case study，论文叙事仍以仿真为主、真机作为 preliminary real-world results。

P0 第一周（M0 D1-D7）由团队向实验室管理员人工确认以下 5 项信息：1) 真机具体型号（如 SO-100 / Franka FR3 / xArm7 / AgileX Piper / UR5 等）；2) 借用时段与可用频率（每周可用几天 / 是否需要排队 / 寒暑假是否可用）；3) SDK 兼容性（LeRobot 主线原生支持型号 vs 需要自实现 adapter）；4) 第三视角相机型号（RealSense / Logitech / 普通 USB）；5) 工作台环境（尺寸、背景/光照可控性、与机器人物理距离）。以上 5 项信息必须在 P0 第一周（M0 D1-D7）由团队向实验室管理员人工确认；若 SDK 兼容性确认为需自实现 adapter，P0 工程预算 +1-2 周，P1 真机采集时段相应弹性扩展。

P0 D5-D6 必须由团队向学校 IT/院系存储管理员人工确认共享 GPU 与共享存储配额，特别是是否能容纳 FastVGGT 离线缓存 325 GB（fp8 量化版）；若配额不足，启动 revision_plan.md §1.3 备选方案（单 suite 缓存 / 阿里云 OSS 冷存储 / streaming 推理）。

### 6.6 R6 · LIBERO-Plus 评测耗时超 GPU 预算

严重度低，概率约 50%。触发条件是 P2 末 GPU 小时已用 200+。缓解措施：分批跑（每天 1-2 维度）；优先跑最可能验证 H1 的 viewpoint 与 state 维度；将 instance 数从 300 降到 200（power 仍 0.6+）；将 stretch ablation 推到论文 stretch 章节不进主对比。

---

## §7 参考文献

[1] Kim, M. J., Finn, C., & Liang, P. (2025). *Fine-Tuning Vision-Language-Action Models: Optimizing Speed and Success*. RSS 2025. arXiv:2502.19645.

[2] Li, F., Song, W., Zhao, H., Wang, J., Ding, P., Wang, D., Zeng, L., & Li, H. (2025). *Spatial Forcing: Implicit Spatial Representation Alignment for Vision-Language-Action Model*. arXiv:2510.12276 (ICLR 2026 under review).

[3] Zhang, J., Wu, S., Luo, X., Wu, H., Gao, L., Shen, H. T., & Song, J. (2025). *InSpire: Vision-Language-Action Models with Intrinsic Spatial Reasoning*. arXiv:2505.13888.

[4] Shen, Y., Zhang, Z., Qu, Y., Zheng, X., Ji, J., Zhang, S., & Cao, L. (2025). *FastVGGT: Training-Free Acceleration of Visual Geometry Transformer*. arXiv:2509.02560.

[5] Fei, S., Wang, S., Shi, J., Dai, Z., Cai, J., Qian, P., Ji, L., He, X., Zhang, S., Fei, Z., Fu, J., Gong, J., & Qiu, X. (2025). *LIBERO-Plus: In-Depth Robustness Analysis of Vision-Language-Action Models*. arXiv:2510.13626.

[6] Wang, J., Chen, M., Karaev, N., Vedaldi, A., Rupprecht, C., & Novotny, D. (2025). *VGGT: Visual Geometry Grounded Transformer*. CVPR 2025. arXiv:2503.11651.

[7] Zhang, Y., Yuan, W., Zhang, Y., Zhang, X., & Wan, J. (2026). *FocusVLA: Focused Visual Utilization for Vision-Language-Action Models*. arXiv:2603.28740.

[8] Yurchyk, H., Chang, W.-D., Dudek, G., & Meger, D. (2025). *Large Pre-Trained Models for Bimanual Manipulation in 3D*. Humanoids 2025. arXiv:2509.20579.

[9] Liu, B., Zhu, Y., Gao, C., Feng, Y., Liu, Q., Zhu, Y., & Stone, P. (2023). *LIBERO: Benchmarking Knowledge Transfer for Lifelong Robot Learning*. arXiv:2306.03310.

[10] Gu, Q., Ju, Y., Sun, S., Gilitschenski, I., Nishimura, H., Itkina, M., & Shkurti, F. (2025). *SAFE: Multitask Failure Detection for Vision-Language-Action Models*. NeurIPS 2025. arXiv:2506.09937.

[11] Tang, Y., Wang, T., Chen, Y., Zhang, B., Guan, Q., & Tang, R. (2026). *Shifting Uncertainty to Critical Moments: Towards Reliable Uncertainty Quantification for VLA Model*. arXiv:2603.18342.

[12] Qu, D., Song, H., Chen, Q., Yao, Y., Ye, X., Ding, Y., Wang, Z., Gu, J., Zhao, B., Wang, D., & Li, X. (2025). *SpatialVLA: Exploring Spatial Representations for Visual-Language-Action Model*. arXiv:2501.15830.

[13] Yu, B., Lian, S., Lin, X., Shen, Z., Wei, Y., Liu, H., Wu, C., Yuan, H., Wang, B., Huang, C., & Chen, K. (2026). *3D-Mix for VLA: A Plug-and-Play Module for Integrating VGGT-based 3D Information into Vision-Language-Action Models*. arXiv:2603.24393.

[14] Rao, Z., Chen, W., Xie, L., Hua, X., Yin, D., Tian, Z., & Yu, F. R. (2026). *AugVLA-3D: Depth-Driven Feature Augmentation for Vision-Language-Action Models*. ICRA 2026. arXiv:2602.10698.

[15] Yuan, T., Liu, Y., Lu, C., Chen, Z., Jiang, T., & Zhao, H. (2025). *DepthVLA: Enhancing Vision-Language-Action Models with Depth-Aware Spatial Reasoning*. arXiv:2510.13375.

[16] Zhuo, D., Zheng, W., Guo, J., Wu, Y., Zhou, J., & Lu, J. (2025). *Streaming 4D Visual Geometry Transformer*. arXiv:2507.11539.

[17] Wang, Y., Zhou, J., Zhu, H., Chang, W., Zhou, Y., Li, Z., Chen, J., Pang, J., Shen, C., & He, T. (2025). *π³: Permutation-Equivariant Visual Geometry Learning*. arXiv:2507.13347.

[18] Ge, S., Zhang, Y., Xie, S., Zhang, W., Zhou, M., & Wang, Z. (2025). *VGGT-DP: Generalizable Robot Control via Vision Foundation Models*. arXiv:2509.18778.

[19] Grislain, C., Rahimi, H., Sigaud, O., & Chetouani, M. (2025). *I-FailSense: Towards General Robotic Failure Detection with Vision-Language Models*. arXiv:2509.16072.

[20] Xing, Y., Luo, X., Xie, J., Gao, L., Shen, H., & Song, J. (2025). *Shortcut Learning in Generalist Robot Policies: The Role of Dataset Diversity and Fragmentation*. CoRL 2025. arXiv:2508.06426.

[21] Guo, J., Wu, Z., Tu, C., Ma, Y., Kong, X., Liu, Z., Ji, J., Zhang, S., Chen, Y., Chen, K., Dou, Q., Yang, Y., Liu, X., Zhao, H., Lv, W., & Li, S. (2026). *On Robustness of Vision-Language-Action Model against Multi-Modal Perturbations*. arXiv:2510.00037.

[22] Oquab, M., Darcet, T., Moutakanni, T., Vo, H., Szafraniec, M., Khalidov, V., et al. (2024). *DINOv2: Learning Robust Visual Features without Supervision*. arXiv:2304.07193.
